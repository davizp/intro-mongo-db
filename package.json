{
  "name": "intro-to-mongodb",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Scott Moss <willscottmoss@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "docker:start": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:stop": "docker-compose -f docker-compose.dev.yml down",
    "docker:kill": "docker-compose -f docker-compose.dev.yml rm -f"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "cuid": "^2.1.4",
    "express": "^4.16.4",
    "mongoose": "^5.3.2",
    "morgan": "^1.9.1"
  },
  "jest": {
    "verbose": true,
    "testURL": "http://localhost/",
    "testEnvironment": "node",
    "setupTestFrameworkScriptFile": "<rootDir>/testconfig.js",
    "restoreMocks": true,
    "testPathIgnorePatterns": [
      "dist/"
    ]
  },
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
